{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "explanation"],
  "properties": {
    "status": { "type": "string", "enum": ["accepted", "rejected", "needs_user_confirmation"] },
    "explanation": { "type": "string" }
  }
}
